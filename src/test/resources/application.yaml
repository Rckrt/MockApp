application:
  static:
    resources:
      patterns: "static resources/patterns"
      scenarios: "static resources/scenarios"
      scripts: "static resources/scripts"
      templates: "static resources/templates"
server:
  port: 8080
  compression:
    enabled: true
    min-response-size: 1
  jetty:
    acceptors: 2

